import React from 'react';
import { history, ui } from "../store";

import styles from './Form.module.scss';

const Form: React.FC = () => {
  return (
    <form>
    </form>  
  );
}

export default Form;
